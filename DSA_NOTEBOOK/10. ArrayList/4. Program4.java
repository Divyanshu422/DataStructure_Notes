
// import java.util.ArrayList;

// class Main {
// public static void main(String[] args) {

// ArrayList<Integer> list = new ArrayList<>();
// list.add(2);
// list.add(17);
// list.add(36);
// list.add(15);
// list.add(9);

// int max = Integer.MIN_VALUE; // This statement will store the -infinity to
// the max variable
// for (int i = 0; i < list.size(); i++) {
// if (max < list.get(i)) {
// max = list.get(i);
// }
// //! Rather than using the if statement we can use the math operator also
// which
// // will store the value in max variable as shown below:
// //* max = Math.max(max, list.get(i));
// }
// // Printing the maximum element
// System.out.println("the maximum element is " + max);

// }
// }
